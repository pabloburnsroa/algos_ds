// Priority Queue
// Data structire where each element has a priority. Higher priority el's are served before those with lower priorities.

// Naive approach
// Iterate over a list of elements to find the highest priority

// We can use a heap instead

// Pseudocode



