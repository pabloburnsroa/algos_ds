/**
 * 
 * @param {*} root 
 * @returns 
 * @description Given an N-ary tree, return its maximum depth.
Note: An N-ary tree is a tree in which any node may have at most N children.

Ex: Given the following tree…

            4
          / | \
         3  9  2
        /       \
       7         2
return 3.
 */

function divingDeep(root) {
  if (!root) return 0;
}
