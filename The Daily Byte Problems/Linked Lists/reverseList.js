/**
 *
 * @param {ListNode} head
 * @returns {ListNode}
 * @readonly Given a linked list, containing unique values, reverse it, and return the result.
 */

function ReverseList(head) {
  let 
}

function ListNode(val, next) {
  this.val = val === undefined ? 0 : val;
  this.next = next === undefined ? null : next;
}
